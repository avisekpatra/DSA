#include<iostream>
using namespace std;

//typedef is use to just rename the data type for the use convieniet what the ariabel is used for

typedef float marks;
typedef int rollno;
 int main()
 {
marks mi,m2,m3,m4; //see we use marks as a data type to declare this set of variabel but still this are float
rollno r1,r2,r3,r4;//we use the word rollno but still these are int


     return 0;
 } 