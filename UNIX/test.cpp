#include<bits/stdc++.h>
using namespace std;

int com(int n)
{
    int num=0;
    int count=0;
    while (n!=0)
    { 
    int ld=n%2;
    if(ld==1)
    num=num+0
    else
    num=num+math.pow(2,count);

    num/=10;
    count++;
    }
    
   
}