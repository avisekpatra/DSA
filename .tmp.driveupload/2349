#include<bits/stdc++.h>
using namespace std;

int fact(int n)
{
    if(n<=1)
    return 1;
    else
    return fact(n-1)*n;
}

int ncr(int n,int r)
{
    return fact(n)/(fact(r)*fact(n-r));
}

int main()
{
    cout<<ncr(4,2)<<endl;
}
